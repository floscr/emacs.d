# -*- mode: snippet -*-
# name: reactClassComponent
# key: rcs
# --
class ${1:${`(file-name-base (buffer-file-name))`}} extends React.Component {
  static propTypes = {
    $2
  };

  constructor() {
      super();
      this.state = {

      };
  }

  render() {
    const { $3 } = this.props;
    return (
      $0
    );
  }
}

export default $1;
