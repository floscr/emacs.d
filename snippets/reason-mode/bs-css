# -*- mode: snippet -*-
# name: BsCss
# key: css
# --
module Styles = {
  open Css;

  let root =
    style([
      display(flexBox),
      flexDirection(column),
      flexGrow(1.),
      flexShrink(1.),
    ]);
};